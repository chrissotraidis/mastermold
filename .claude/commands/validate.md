Run the full validation suite:

1. Run `bin/task verify` to execute all config.yml verification checks.
2. Report each check: name, pass/fail, required status.
3. If any required checks fail, investigate and suggest fixes.
4. Summarize: total checks, passed, failed, required failures.
